# idOS MetaMask Snap

```shell
yarn install && yarn start
```

This snap was used as a source of entropy for the user's encryption key (and as a trusted execution environment).

We abandoned this approach since it makes it impossible for users to use wallets in non-EVM ecosystems, which is anathema to idOS's goals.
